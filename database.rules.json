{
  "rules": {
    ".read": false,
    ".write": false,

    "status": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users/' + auth.uid + '/role').val() == 'owner' || root.child('users/' + auth.uid + '/role').val() == 'admin' || root.child('users/' + auth.uid + '/role').val() == 'instructor' || root.child('users/' + auth.uid + '/role').val() == 'ta')",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "enrollments": {
      "$uid": {
        "$courseId": {
          ".read": "auth != null && auth.uid === $uid",
          ".write": "auth != null && auth.uid === $uid && newData.isBoolean()"
        }
      }
    },

    "examStatus": {
      "$courseId": {
        ".read": "auth != null && (root.child('users/' + auth.uid + '/role').val() == 'owner' || root.child('users/' + auth.uid + '/role').val() == 'admin' || root.child('users/' + auth.uid + '/role').val() == 'instructor' || root.child('users/' + auth.uid + '/role').val() == 'ta')",
        ".write": "auth != null && (root.child('users/' + auth.uid + '/role').val() == 'owner' || root.child('users/' + auth.uid + '/role').val() == 'admin' || root.child('users/' + auth.uid + '/role').val() == 'instructor' || root.child('users/' + auth.uid + '/role').val() == 'ta') && newData.isBoolean()"
      }
    },

    "chats": {
      "$courseId": {
        ".read": "auth != null && root.child('enrollments/' + auth.uid + '/' + $courseId).val() === true",
        ".write": "auth != null && root.child('enrollments/' + auth.uid + '/' + $courseId).val() === true && !root.child('examStatus/' + $courseId).val()",

        "$msgId": {
          ".validate": "newData.hasChildren(['uid','user','text','ts']) && newData.child('uid').val() === auth.uid && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500 && newData.child('ts').isNumber()"
        }
      }
    },

    "announcements": {
      ".read": true,
      "$id": {
        ".write": "auth != null && (root.child('users/' + auth.uid + '/role').val() == 'owner' || root.child('users/' + auth.uid + '/role').val() == 'admin')",
        ".validate": "newData.hasChildren(['title','body','ts'])"
      }
    }
  }
}