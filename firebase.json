{
  "hosting": {
    "public": "public",
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  },
  "firestore": { "rules": "firestore.rules" },
  "storage": [{ "target": "prod", "rules": "storage.rules" }],
  "database": { "rules": "database.rules.json" }
}
